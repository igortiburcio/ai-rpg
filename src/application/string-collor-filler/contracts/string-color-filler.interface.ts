export abstract class IStringColorFiller {
  abstract purple(text: string): string;
  abstract yellow(text: string): string;
  abstract red(text: string): string;
  abstract green(text: string): string;
  abstract blue(text: string): string;
  abstract brown(text: string): string;
  abstract pink(text: string): string;
  abstract white(text: string): string;
  abstract orange(text: string): string;
}
